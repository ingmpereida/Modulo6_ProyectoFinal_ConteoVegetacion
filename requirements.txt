# Dependencias Python de tile_pipeline.py (Fase 2 - preparacion de tiles)
# Instalar con: pip install -r requirements.txt
Pillow>=10.0
numpy>=1.26

# YOLO training pipeline (prepare_dataset.py + suite pytest, FR-6)
# Versiones elegidas el 2026-09-23; ultralytics se instala y verifica en la tarea 3.1 (gate F-1)
pytest==9.1.1
PyYAML==6.0.3
ultralytics==8.4.161